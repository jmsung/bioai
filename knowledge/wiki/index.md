---
type: catalog
author: agent
---

# Index

Catalog of every page in `wiki/` and every distillation in `source/`.
The agent updates this on every ingest. To search by content, use
`/wiki-query` or `qmd query "<question>" -c precision-health-agents-kb-source`.

## Synthesis pages (`wiki/`)

### Concepts / mechanisms

*(empty — populate as the kb grows.)*

### Findings / decisions

*(empty.)*

### Personas / techniques / problems / questions

*(empty.)*

## Distillations (`source/`)

*(none yet — `/wiki-ingest <url>` to start.)*
